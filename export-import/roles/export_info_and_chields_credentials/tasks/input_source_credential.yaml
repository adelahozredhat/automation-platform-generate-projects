- name: Iterate Input SOurce Credential
  block:
  - name: Set facts
    set_fact:
      export_output_source_credential: "{{ export_output_source_credential| union([source_credential_vaults[item.name]])}}"
  when: item.name in source_credential_vaults
